README.security.md

Cache/version: keep `update.html`, page `?v=`, footer `__ver`, and `sw.js` CACHE on the **same** number (currently v77).
Do not half-bump: a mismatched `update.html` redirect causes a loop on phones.
